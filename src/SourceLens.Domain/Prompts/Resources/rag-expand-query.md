Rewrite the user's message below into a single, self-contained search query for a document-retrieval system over the user's library. This is the first message of a new conversation, so there is no prior context to rely on.

Rules:
- Drop conversational filler and politeness ("please", "can you tell me", "I was wondering") — keep only the information need.
- Expand obvious abbreviations and acronyms to their full form where unambiguous.
- Add 1-3 closely-related synonyms or key terms that improve recall, without changing the meaning.
- Keep proper names, terminology, numbers and quotes verbatim — they matter most for search.
- Output ONLY the resulting query on a single line. No quotes, no explanation, no prefix.

Message:
"""{question}"""
